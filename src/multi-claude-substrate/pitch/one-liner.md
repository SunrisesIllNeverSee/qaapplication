# One-liner

> **Multi-AI coordination doesn't need a new transport. It needs a lane
> discipline. Three text files in a repo, and `git` is the audit log.**

## Tighter variants

- "We coordinate parallel Claude / Codex sessions with three text files
  and an append-only thread. No bus, no broker."
- "The interesting layer in multi-agent infra isn't message passing —
  it's lane claims, truth ledgers, and git as audit."
- "Filesystem is already the bus. We built the discipline on top."
- "Substrate-level coordination for multi-AI: lane discipline + truth
  ledger + git as audit. Load-tested by parallel sessions in
  production."

## When you only have 8 seconds

> "Multi-Claude coordination is just three text files and a git log.
> No bus. No broker. We have receipts."
